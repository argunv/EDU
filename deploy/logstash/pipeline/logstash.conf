input {
  tcp {
    host => "0.0.0.0"
    port => 5000
    codec => json_lines
  }
}

output {
  elasticsearch {
    hosts    => ["http://elasticsearch:9200"]
    index    => "docker-logs-%{+YYYY.MM.dd}"
    ilm_enabled => false
    manage_template => false
  }
}
